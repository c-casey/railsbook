<%= turbo_frame_tag "#{likeable.class.to_s.downcase}_#{likeable.id}_like_form" do %>
  <%= button_to like_button_text(likeable), delete_like_path(likeable), method: :delete, id: "#{likeable.class.to_s.downcase}_#{likeable.id}_like_button", class: "button is-success" %>
<% end %>
